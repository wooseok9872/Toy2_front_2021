<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="70dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/back_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginLeft="10dp"
            android:src="@drawable/free_icon_left_arrow_in_circular_button_black_symbol_54476" />

        <androidx.appcompat.widget.SearchView
            android:id="@+id/friend_search_input"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            app:iconifiedByDefault="false"
            app:queryBackground="@drawable/search_view_round"
            app:queryHint="추가할 닉네임을 입력하세요" />

        <ImageView
            android:id="@+id/friend_search_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/free_icon_magnifying_glass_search_16492" />
    </LinearLayout>


    <ListView
        android:id="@+id/friend_add_listView"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginRight="20dp">
    </ListView>

    <Button
        android:id="@+id/friend_add_button"
        android:layout_width="170dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="40dp"
        android:backgroundTint="@color/black"
        android:text="@string/add_button"
        android:textStyle="bold" />

</LinearLayout>